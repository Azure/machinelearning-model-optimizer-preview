# Before run optimizer job

* Pip requirements
    Install the necessary python packages:

    ```
    python -m pip install -r requirements.txt
    ```
* Prepare data and model
    ```
    python prepare_model_data.py
    ```